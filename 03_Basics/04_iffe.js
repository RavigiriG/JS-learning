// Immediately Invoked Function Expressions (IIFE)


(function chai(){
    console.log(`DB CONECTED`);
})();                           // Give semicolan to end the iffe.

( () => {
    console.log(`DB CONNECTED 2`);
})();
